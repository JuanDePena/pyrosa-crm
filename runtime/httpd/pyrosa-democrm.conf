<VirtualHost *:80>
  ServerName democrm.pyrosa.com.do

  RewriteEngine On
  RewriteCond %{REQUEST_URI} !^/\.well-known/acme-challenge/
  RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [R=301,L,NE]
</VirtualHost>

<IfModule mod_ssl.c>
<VirtualHost *:443>
  ServerName democrm.pyrosa.com.do

  SSLEngine on
  SSLCertificateFile /etc/ssl/simplehostman/pyrosa.com.do/fullchain.pem
  SSLCertificateKeyFile /etc/ssl/simplehostman/pyrosa.com.do/privkey.pem

  Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains"
  Header always set X-Content-Type-Options "nosniff"
  Header always set X-Frame-Options "SAMEORIGIN"
  Header always set Referrer-Policy "strict-origin-when-cross-origin"

  RequestHeader set X-Forwarded-Proto "https"
  RequestHeader set X-Forwarded-Port "443"
  ProxyPreserveHost On
  ProxyRequests Off
  ProxyPass / http://127.0.0.1:10166/ retry=0 timeout=120
  ProxyPassReverse / http://127.0.0.1:10166/

  ErrorLog /var/log/httpd/pyrosa-democrm_ssl_error.log
  CustomLog /var/log/httpd/pyrosa-democrm_ssl_access.log combined
</VirtualHost>
</IfModule>
